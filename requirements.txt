# Web framework
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.18

# Database
sqlalchemy==2.0.36
alembic==1.14.0
psycopg2-binary==2.9.10

# Validation
pydantic==2.10.3
pydantic-settings==2.7.0

# Auth - Google & custom JWT
# The [requests] extra is load-bearing: core/security.py uses the requests
# transport to verify Google ID tokens, and google-auth treats requests as
# optional. It used to arrive transitively via youtube-transcript-api, so
# dropping that package took it with it and broke startup.
google-auth[requests]==2.30.0
pyjwt[crypto]==2.10.1
python-jose[cryptography]==3.3.0
httpx==0.28.1

# AI notes generation
groq==0.25.0
openai==1.58.0

# Utilities
structlog==24.4.0
python-dotenv==1.0.1
aiofiles==24.1.0

# Testing
pytest==8.3.4
pytest-asyncio==0.24.0
httpx==0.28.1

# Added dependencies
email-validator
